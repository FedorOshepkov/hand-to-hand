import React from 'react';
import './PlayButtonComponent.css';

function PlayButton() {
  const playButton = <button type="button" className="button">Play</button>;

  return (
    playButton
  );
}

export default PlayButton;
